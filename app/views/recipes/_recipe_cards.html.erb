<div class="container-fluid mt-2 mx-3">
  <div id="recipes" class="row row-cols-1 row-cols-md-4 g-4" data-controller='recipes'>
    <% recipes.each do |recipe| %>
      <%= render partial: "recipe-card", locals: { recipe: recipe } %>
    <% end %>
  </div>
</div>